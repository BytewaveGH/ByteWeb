import React from 'react'

export const Deadlines = ({ setItemsx, addTab }: { setItemsx?: (e: any) => void, addTab?: () => void }) => {
    return (
        <div>
            deadlines

            <button>gfxf</button>
        </div>
    )
}
